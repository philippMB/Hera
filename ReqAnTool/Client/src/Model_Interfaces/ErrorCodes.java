package Model_Interfaces;

public enum ErrorCodes
{
    NO_ERROR,
    REFERENCES_NOT_SOLVED,
    LIST_OVERFLOW,
    FORMAT_MISMATCH,
    DUPLICATE,
    NOT_EXISTENT,
    NO_COST_ESTIMATION,
    INVALID_ARGUMENT,
    REFERENCES_ON_ITEM_DELETED,
    
}
